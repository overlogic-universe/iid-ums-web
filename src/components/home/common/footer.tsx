import { NextPage } from "next";

interface Props {}

const Footer: NextPage<Props> = ({}) => {
  return <div className="bg-red-500 w-full h-48 flex items-center justify-center">This is Footer</div>;
};

export default Footer;
